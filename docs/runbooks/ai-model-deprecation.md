# Runbook: AI Model Deprecation

Discover current provider models, mark the old local model deprecated, verify a replacement's capabilities, approve it by environment, create and evaluate a new route version, activate with separation of duties, monitor fallback/quality/cost, and retain the previous route for governed rollback. Never rewrite historical provenance.
